## What I Learned

- How to get numeric input from the user with `input()` and convert it using `float()`
- How to use a simple `if / else` structure to make a binary decision
- How to combine multiple `print()` statements to give a more detailed message
- How to work with real-world thresholds (like body temperature) in conditional logic
